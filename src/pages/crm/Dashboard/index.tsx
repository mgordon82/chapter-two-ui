import { Box, Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <Box>
      <Typography>Welcome to the Coach Dashboard</Typography>
    </Box>
  );
};

export default Dashboard;
