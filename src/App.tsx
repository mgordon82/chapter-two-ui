import React from 'react';
import { Box } from '@mui/material';
import AppRoutes from './routes';

const App: React.FC = () => {
  return (
    <Box>
      <AppRoutes />
    </Box>
  );
};

export default App;
